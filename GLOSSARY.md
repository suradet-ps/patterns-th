# พจนานุกรมศัพท์และแนวทางการแปล (Glossary & Guidelines) — Rust Design Patterns ฉบับภาษาไทย

เอกสารฉบับนี้กำหนดมาตรฐานคำศัพท์และแนวทางการแปลภาษาไทยสำหรับหนังสือ **Rust Design Patterns** เพื่อให้เนื้อหาทั้ง 50 บทมีความสม่ำเสมอ อ่านลื่นไหลเป็นธรรมชาติสำหรับโปรแกรมเมอร์ไทย และคงความถูกต้องแม่นยำตามต้นฉบับภาษาอังกฤษ 100%

---

## 1. ตารางคำศัพท์มาตรฐาน (Standard Terminology)

### 1.1 แนวคิดหลักและกลไกของภาษา Rust (Core Rust Concepts & Mechanisms)

| ศัพท์ต้นฉบับ | คำแปลไทย / คำทับศัพท์ | แนวทางการใช้และหมายเหตุ |
|---|---|---|
| ownership | ความเป็นเจ้าของ (ownership) | ระบบความเป็นเจ้าของหน่วยความจำของ Rust |
| borrowing | การยืม (borrowing) | การยืมข้อมูลผ่านการอ้างอิง |
| borrowed type | ไทป์แบบยืม (borrowed type) | เช่น `&str`, `&[T]`, `&T` |
| owned type | ไทป์ที่เป็นเจ้าของข้อมูล (owned type) | เช่น `String`, `Vec<T>`, `Box<T>` |
| borrow checker | borrow checker | ระบบตรวจสอบการยืมข้อมูลของคอมไพเลอร์ คงรูปทับศัพท์ภาษาอังกฤษ |
| lifetime | ไลฟ์ไทม์ (lifetime) | ช่วงอายุขัยหรือขอบเขตเวลาที่ข้อมูลมีผลใช้งานได้ |
| move semantics | move semantics | พฤติกรรมการย้ายข้อมูลหรือโอนย้ายสิทธิ์ความเป็นเจ้าของ |
| copy semantics | copy semantics | พฤติกรรมการทำสำเนาข้อมูลแบบบิตต่อบิต |
| mutable / immutable | ที่เปลี่ยนแปลงได้ / ที่เปลี่ยนแปลงไม่ได้ (mutable / immutable) | เลือกใช้ "เปลี่ยนแปลงได้/ไม่ได้" หรือทับศัพท์ "มิวเทเบิล/อิมมิวเทเบิล" ตามความลื่นไหลของประโยค |
| interior mutability | การเปลี่ยนแปลงค่าจากภายใน (interior mutability) | กลไกที่ยอมให้แก้ไขค่าได้แม้จะอยู่หลัง immutable reference (เช่น `RefCell`, `Mutex`) |
| zero-cost abstraction | zero-cost abstraction | แอบสแตรกชันที่ไร้ต้นทุนหรือภาระตอนรันไทม์ (คอมไพล์แล้วมีประสิทธิภาพเท่ากับการเขียนโค้ดระดับล่างด้วยตนเอง) |
| RAII | RAII | Resource Acquisition Is Initialization (การผูกอายุและการคืนทรัพยากรไว้กับขอบเขตตัวแปร) |
| destructor / drop | เดสทรัคเตอร์ (destructor) / การคืนทรัพยากร (drop) | การทำลายออบเจกต์และปล่อยทรัพยากรเมื่อพ้นขอบเขต |
| deref coercion | deref coercion | การบังคับแปลงไทป์อัตโนมัติผ่าน trait `Deref` |
| fat pointer | fat pointer | พอยน์เตอร์ขนาดกว้างที่เก็บข้อมูลพร้อม metadata (เช่น ตัวชี้ข้อมูล + ความยาว หรือ ตัวชี้ข้อมูล + vtable) **ห้ามแปลว่า "พอยน์เตอร์แบบอ้วน"** |
| smart pointer | สมาร์ตพอยน์เตอร์ (smart pointer) | ชนิดข้อมูลที่ทำหน้าที่เป็นพอยน์เตอร์พร้อมเมทาดาทาและฟังก์ชันเพิ่มเติม (เช่น `Box`, `Rc`, `Arc`) |
| dynamic dispatch | ไดนามิกดิสแพตช์ (dynamic dispatch) | การเลือกเมท็อดที่จะเรียกใช้งานตอนรันไทม์ (ผ่าน trait object `dyn Trait`) |
| static dispatch | สแตติกดิสแพตช์ (static dispatch) | การระบุเมท็อดที่จะเรียกใช้งานตั้งแต่ตอนคอมไพล์ (ผ่าน Generics / Monomorphization) |
| trait bound | trait bound | ข้อกำหนดหรือเงื่อนไขขอบเขตของ trait บน generic type |
| type safety | ไทป์เซฟตี (type safety) | ความปลอดภัยของระบบชนิดข้อมูล ป้องกันข้อผิดพลาดตั้งแต่ตอนคอมไพล์ |
| type inference | การอนุมานไทป์ (type inference) | ความสามารถของคอมไพเลอร์ในการเดาชนิดข้อมูลได้เองโดยไม่ต้องระบุชัดเจน |
| type alias | type alias | การตั้งชื่อทางเลือกให้ไทป์ด้วยคีย์เวิร์ด `type` |
| closure | โคลเชอร์ (closure) | ฟังก์ชันนิรนามที่สามารถจับ (capture) ตัวแปรจากสภาพแวดล้อมรอบข้างได้ |
| iterator | อิเทอเรเตอร์ (iterator) | ตัววนลูปข้อมูลตาม trait `Iterator` |
| privacy / visibility | การควบคุมระดับการเข้าถึง (privacy / visibility) | การกำหนดการมองเห็นของไอเทมในโมดูล เช่น `pub` หรือ private **ห้ามแปลว่า "ความเป็นส่วนตัว"** เพื่อไม่ให้สับสนกับนโยบายข้อมูลส่วนบุคคล |
| unsafe | unsafe | โค้ดที่คอมไพเลอร์ไม่สามารถรับประกันความปลอดภัยของหน่วยความจำให้ได้ คงรูปคำสงวน |
| FFI | FFI | Foreign Function Interface (อินเทอร์เฟซเรียกใช้งานฟังก์ชันข้ามภาษา) |
| undefined behavior (UB) | พฤติกรรมที่ไม่ได้นิยาม (Undefined Behavior - UB) | สถานะที่ภาษาไม่รับประกันผลลัพธ์การทำงาน อาจเกิดข้อผิดพลาดร้ายแรง |

---

### 1.2 หมวดหมู่ดีไซน์แพตเทิร์นและชื่อแพตเทิร์น (Patterns, Idioms & Anti-patterns)

| ศัพท์ต้นฉบับ | คำแปลไทย / คำทับศัพท์ | แนวทางการใช้และหมายเหตุ |
|---|---|---|
| idiom | สำนวนการเขียนโค้ด (idiom) / แนวปฏิบัติเฉพาะภาษา (idiom) | **หลีกเลี่ยงการแปลว่า "สำนวน" โดดๆ** เพราะอาจทำให้เข้าใจผิดว่าเป็นสำนวนภาษาหรือสุภาษิต |
| design pattern | ดีไซน์แพตเทิร์น (design pattern) | รูปแบบการออกแบบหรือโซลูชันมาตรฐานที่นำกลับมาใช้ซ้ำได้สำหรับปัญหาที่พบบ่อย |
| anti-pattern | แอนติแพตเทิร์น (anti-pattern) | รูปแบบที่ไม่ควรทำ หรือรูปแบบที่ดูเหมือนแก้ปัญหาแต่แท้จริงกลับสร้างปัญหาเพิ่มขึ้น |
| creational pattern | แพตเทิร์นเชิงการสร้าง (creational pattern) | รูปแบบการออกแบบเกี่ยวกับการสร้างออบเจกต์ |
| structural pattern | แพตเทิร์นเชิงโครงสร้าง (structural pattern) | รูปแบบการออกแบบเกี่ยวกับการประกอบ struct และโมดูล |
| behavioural pattern | แพตเทิร์นเชิงพฤติกรรม (behavioural pattern) | รูปแบบการออกแบบเกี่ยวกับการสื่อสารและการกระจายความรับผิดชอบระหว่างคอมโพเนนต์ |
| Builder | บิลเดอร์ (Builder pattern) | แพตเทิร์นสร้างออบเจกต์ที่มีความซับซ้อนทีละขั้นตอน |
| Command | คอมมานด์ (Command pattern) | แพตเทิร์นแปลงคำสั่งการทำงานให้อยู่ในรูปออบเจกต์ **ไม่แปลโดดๆ ว่า "คำสั่ง"** เพื่อไม่ให้สับสนกับ statement |
| Interpreter | อินเทอร์พรีเตอร์ (Interpreter pattern) | แพตเทิร์นการแปลความหมายภาษาหรือนิพจน์ |
| Newtype | นิวไทป์ (Newtype pattern) | แพตเทิร์นการสร้างไทป์ใหม่ด้วย tuple struct 1 ฟิลด์เพื่อความปลอดภัยและการซ่อนรายละเอียด |
| RAII Guard | RAII Guard | การ์ดดูแลการคืนทรัพยากรตามขอบเขตตัวแปร (เช่น `MutexGuard`) |
| Strategy | สแตรทิจี (Strategy pattern) | แพตเทิร์นการสลับเปลี่ยนอัลกอริทึมในการทำงาน |
| Visitor | วิสิเตอร์ (Visitor pattern) | แพตเทิร์นการแยกขั้นตอนการทำงานออกจากโครงสร้างข้อมูล |
| Fold | โฟลด์ (Fold pattern) | แพตเทิร์นการแปลงและรวบรวมโครงสร้างข้อมูล |
| wrapper | ตัวห่อหุ้ม (wrapper) | ออบเจกต์หรือไทป์ที่นำมาครอบข้อมูลอีกชั้นหนึ่ง |
| opaque wrapper | ตัวห่อหุ้มที่ซ่อนรายละเอียดภายใน (opaque wrapper) | **ห้ามแปลว่า "ตัวห่อทึบแสง"** |

---

### 1.3 สถาปัตยกรรมและวิศวกรรมซอฟต์แวร์ (Software Engineering & Architecture)

| ศัพท์ต้นฉบับ | คำแปลไทย / คำทับศัพท์ | แนวทางการใช้และหมายเหตุ |
|---|---|---|
| trade-off | ข้อแลกเปลี่ยน / สิ่งที่ต้องชั่งน้ำหนัก (trade-off) | ข้อดีข้อเสียที่ต้องพิจารณาเมื่อเลือกโซลูชันหนึ่ง |
| abstraction | แอบสแตรกชัน (abstraction) / การสร้างนามธรรม | การซ่อนรายละเอียดการทำงานภายในและเปิดเผยเฉพาะส่วนที่จำเป็นต่อการใช้งาน |
| indirection | การอ้างอิงทางอ้อม (indirection) | การเข้าถึงข้อมูลผ่านพอยน์เตอร์หรือตัวแปรคั่นกลาง **ห้ามแปลว่า "ชั้นของการอ้อมผ่าน"** |
| boilerplate | โค้ดส่วนเกินที่ซ้ำซากจำเจ (boilerplate code) | โค้ดพื้นฐานจำนวนมากที่ต้องเขียนซ้ำๆ เพื่อให้ระบบทำงานได้ |
| ergonomics | ความสะดวกและเป็นธรรมชาติในการเขียนโค้ด (ergonomics) | ความง่ายดายและเป็นมิตรของ API ในมุมมองของผู้เรียกใช้งาน |
| convention | ธรรมเนียมปฏิบัติ (convention) | ข้อตกลงร่วมกันของชุมชนนักพัฒนา |
| overhead | โอเวอร์เฮด (overhead) | ภาระการทำงานส่วนเกินของระบบ (เช่น ซีพียู หรือหน่วยความจำ) |
| allocation | การจัดสรรหน่วยความจำ (allocation) | การจองพื้นที่บน heap |
| stack / heap | สแตก (stack) / ฮีป (heap) | หน่วยความจำแบบสแตกและฮีป ทับศัพท์ตามสากลนิยม |
| in-place | ในตำแหน่งเดิม (in-place) | การแก้ไขข้อมูล ณ ตำแหน่งหน่วยความจำเดิมโดยไม่ต้องคัดลอกใหม่ |
| backwards compatible | เข้ากันได้ย้อนหลัง (backwards compatible) | ความสามารถในการทำงานร่วมกับโค้ดเวอร์ชันก่อนหน้าได้โดยไม่พัง |
| thread-safe | ปลอดภัยสำหรับการทำงานข้ามเธรด (thread-safe) | โค้ดที่สามารถเรียกใช้งานพร้อมกันจากหลายเธรดได้อย่างถูกต้อง |
| good indication | ข้อบ่งชี้ที่ชัดเจน (good indication) | **ในบริบทของแอนติแพตเทิร์น ห้ามแปลว่า "สัญญาณที่ดี"** เพราะไม่ใช่เรื่องน่ายินดี |
| motivation | ที่มาและเหตุผล (Motivation) | หัวข้อย่อยในแต่ละบทที่อธิบายสาเหตุและประโยชน์ของการใช้แพตเทิร์นนั้นๆ (ดีกว่าแปลว่า "แรงจูงใจ") |
| discussion | เจาะลึกรายละเอียด (Discussion) | หัวข้อย่อยในแต่ละบทที่อธิบายข้อพิจารณาและการใช้งานเชิงลึก (ดีกว่าแปลว่า "การอภิปราย") |
| advantages / disadvantages | ข้อดี / ข้อเสีย | หัวข้อย่อยในแต่ละบท |

---

### 1.4 คีย์เวิร์ด ไวยากรณ์ และคำเฉพาะของ Rust (Keywords & Identifiers — คงรูปภาษาอังกฤษ)

คำเหล่านี้เป็นคีย์เวิร์ด คำสั่ง หรือชื่อสร้างในภาษา Rust จึง**ต้องคงรูปเดิมเสมอ** ไม่แปลเป็นภาษาไทย:

- `trait`, `struct`, `enum`, `impl`, `derive`, `unsafe`
- `fn`, `let`, `mut`, `pub`, `type`, `const`, `static`
- `crate`, `module`, `macro`, `match`, `where`, `self`, `Self`
- ชื่อไทป์พื้นฐานและไลบรารีมาตรฐาน เช่น `Option`, `Result`, `String`, `Vec`, `Box`, `Rc`, `Arc`, `Cell`, `RefCell`
- คำสั่งและเครื่องมือของ Rust เช่น `cargo`, `rustc`, `clippy`, `rustfmt`, `mdbook`

---

## 2. แนวปฏิบัติการแปลเพื่อความลื่นไหลเป็นธรรมชาติ (Fluency & Style Guidelines)

1. **แปลเอาความหมาย (Sense-for-Sense) ไม่แปลคำต่อคำ (Word-by-Word)**:
   - ภาษาอังกฤษชอบใช้รูปประโยคแบบกรรม (Passive Voice) เช่น *"This pattern is used by developers to..."* ให้ปรับเป็นประโยคบอกเล่าภาษาไทยที่เป็นธรรมชาติ เช่น *"นักพัฒนามักใช้แพตเทิร์นนี้เพื่อ..."*
   - หลีกเลี่ยงคำฟุ่มเฟือย เช่น *"ทำการ...", "มีความเป็น...", "ในส่วนของ...", "ถูกทำให้..."* โดยตัดทอนให้กระชับ เช่น *"ทำการคอมไพล์"* ➔ *"คอมไพล์"*

2. **การใช้คำศัพท์เฉพาะทางและการกำกับภาษาอังกฤษ**:
   - เมื่อกล่าวถึงคำศัพท์เทคนิคหรือชื่อแพตเทิร์นเป็นครั้งแรกในแต่ละบท ให้เขียนคำแปลไทยหรือคำทับศัพท์พร้อมวงเล็บภาษาอังกฤษกำกับไว้ เช่น *"การอ้างอิงทางอ้อม (indirection)"*, *"สำนวนการเขียนโค้ด (idiom)"*
   - ในย่อหน้าถัดไป สามารถใช้คำทับศัพท์หรือคำแปลสั้นๆ ได้อย่างเป็นธรรมชาติโดยไม่ต้องใส่วงเล็บซ้ำซ้อน

3. **คำเชื่อมและจังหวะประโยค**:
   - เรียบเรียงประโยคให้อ่านง่าย ชัดเจน ตรงประเด็น เหมาะสำหรับเอกสารอ้างอิงทางวิศวกรรมระดับมืออาชีพ
   - คำเชื่อมภาษาอังกฤษ เช่น *"However"*, *"Furthermore"*, *"In fact"*, *"That being said"* ให้แปลและปรับตำแหน่งคำเชื่อมให้สอดคล้องกับธรรมชาติของภาษาไทย (เช่น *"อย่างไรก็ตาม"*, *"ยิ่งไปกว่านั้น"*, *"อันที่จริง"*)

---

## 3. กฎเหล็กด้านเทคนิค (Non-Negotiable Engineering Constraints)

1. **บล็อกโค้ดห้ามดัดแปลง (Byte-Identical Code Blocks)**:
   - บล็อกโค้ดทุกบล็อก (` ``` ... ``` `) ต้องตรงกับต้นฉบับใน `rust-unofficial/patterns` ทุกตัวอักษร ห้ามแปลข้อความหรือคอมเมนต์ในโค้ดโดยเด็ดขาด เพื่อให้ผ่านการตรวจของ `verify-translation.ps1`
2. **โค้ดอินไลน์ห้ามแปล**:
   - ข้อความที่อยู่ใน inline backticks (เช่น `` `&str` ``, `` `Display` ``, `` `String::from` ``) ต้องคงรูปเดิมตามต้นฉบับเสมอ
3. **ลิงก์และ Anchor ต้องใช้งานได้ 100%**:
   - URL ภายนอกและ Relative path ต้องตรงตามต้นฉบับ
   - Anchor ในหน้า (`#...`) ต้องตรงกับ Heading slug ที่ mdbook สร้างขึ้นจริง (ซึ่ง mdbook จะตัดสระและวรรณยุกต์ไทยออก) และต้องผ่านการตรวจสอบด้วย `check-links.ps1`
4. **โครงสร้างและระดับของหัวข้อ (Headings)**:
   - จำนวนและระดับหัวข้อ (`#`, `##`, `###`) ของแต่ละไฟล์ต้องตรงกับต้นฉบับอย่างสมบูรณ์
